<%= turbo_stream_from "appointments" %>
<div id="appointments" data-controller="sorted">
    <%= render partial: "appointments/appointment", collection: @appointments %>
</div>

<div id="no_appointments_message" class="row live-row">
    <% if @appointments.none? %>
        <div class="col-12 pb-5">
            <h3 class="text-center">Geen afspraken! 😄</h3>
            <h5 class="text-center text-muted fw-light">Iedereen goed bezig...?</h5>
        </div>
    <% end %>
</div>
